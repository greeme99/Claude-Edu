{
  "name": "claude-edu",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix web run build",
    "content": "npm --prefix web run content",
    "content:fetch": "npm --prefix web run content:fetch",
    "content:validate": "npm --prefix web run content:validate",
    "dev": "npm --prefix web run dev",
    "start": "npm --prefix web run start"
  }
}
