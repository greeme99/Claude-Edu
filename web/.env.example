# 시트 ID는 content.config.json 에 있습니다. 시크릿이 아닙니다.
# 이 프로젝트는 자격증명을 사용하지 않습니다 (C-04, NFR-14).
#
# Vercel Deploy Hook URL 은 Apps Script 의 스크립트 속성에 저장합니다.
# 리포에도, 시트 셀에도 두지 않습니다 (R-09-2).

NEXT_PUBLIC_SITE_URL=http://localhost:3000
